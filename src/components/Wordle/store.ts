  // // Get all guess
  // const allguess = () => {
  //   return guess.slice(0, tries).join('').split('');
  // };

  // // Get exact guess
  // const exactguess = () => {
  //   return word.split('').filter((letter, i) => {
  //     return guess.slice(0, tries).map((word) => word[i]).includes(letter);
  //   });
  // };

  // // Get inexact guess
  // const inexactguess = () => {
  //   return word.split('').filter((letter) => allguess().includes(letter));
  // };



  /* <Keyboard guess={allguess()} exactguess={exactguess()} inexactguess={inexactguess()} /> */